# Frontend — Galera

Angular 22 standalone, zoneless, `provideHttpClient(withFetch())`. Routes: auth (`/login`, `/entrar`, `/registro`) and `/pase/:code` **before** shell `**`.

Login: minimalista sin caja, demo creds below form, empty inputs.

QR: `DomSanitizer.bypassSecurityTrustHtml`. 409 copy next to `#pass-action` / `#stamp-action`.

Theme: `galera.theme`, sun/moon toggle with aria-label.
